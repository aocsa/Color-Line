/*CL.widget.menu = (function() {
	var c = { //config
		$box : $(".mediaBox"),
		$video : null,
		$images : null,
		$panorama : null,
		activeMedia : "balle"
	};
	var init = function(conf) {
		$.extend(c, conf);

		
	};
	return{
		init: init,
		config: c
	};
	init();
}());
*/